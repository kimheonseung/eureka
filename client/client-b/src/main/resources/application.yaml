spring:
  application:
    name: eureka-client-b

server:
  port: 8080

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka-server-a:8081/eureka