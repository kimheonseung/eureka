spring:
  application:
    name: eureka-server-b

eureka:
  client:
    fetch-registry: true
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka-server-a:8081/eureka

server:
  port: 8082
